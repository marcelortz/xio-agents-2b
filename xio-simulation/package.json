{
  "name": "xio",
  "version": "0.1.0",
  "description": "XIO agent simulation: governance, ledger, metabolism, genome, agents, arena.",
  "main": "dist/xio-simulation.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/bin/run-simulation.js",
    "dev": "ts-node bin/run-simulation.ts",
    "demo:scale": "ts-node bin/run-large-simulation.ts",
    "pretest": "tsc",
    "test": "node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "@types/node": "^20.14.0"
  }
}
