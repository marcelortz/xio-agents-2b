apiVersion: v1
kind: Service
metadata:
  name: xio-agents-api
  namespace: xio-agents
  labels:
    app: xio-agents-api
spec:
  type: ClusterIP
  selector:
    app: xio-agents-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
