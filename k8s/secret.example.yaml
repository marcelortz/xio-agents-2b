# Template only — do NOT commit real credentials.
# Copy to secret.yaml, fill in real values, and apply that file instead
# (secret.yaml should be gitignored).
apiVersion: v1
kind: Secret
metadata:
  name: xio-agents-db-credentials
  namespace: xio-agents
type: Opaque
stringData:
  DB_USER: "changeme"
  DB_PASSWORD: "changeme"
